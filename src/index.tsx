import React from 'react';
import ReactDOM from 'react-dom';
import SVG from './scripts/components/Opening'
import "./sass/index.scss"

ReactDOM.render(
    <SVG />,
  document.getElementById('app')
);
